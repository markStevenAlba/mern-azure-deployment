const { Account } = require('../models/account');
const { Transaction } = require('../models/transaction');



const getIncomeStatement = async (req, res) => {
    // Implementation for calculating the income statement
};

module.exports = {
    getIncomeStatement,
};